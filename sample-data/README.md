# Sample data

`sales-orders.csv` is the deterministic 2026 sales-order dataset used by the in-app **Use sample sales data** flow. `example-questions.json` contains the same analyst prompts that ship with the application. Regenerate both files after changing the generator with:

```powershell
pnpm sample:export
```
