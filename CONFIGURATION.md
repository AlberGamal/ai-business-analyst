# Local configuration reference

Copy `local.env.template` to `.env` and keep `.env` outside source control. The current local configuration fields, security boundaries, and Windows instructions are maintained in [`README.md`](./README.md) and [`docs/LOCAL_DEVELOPMENT.md`](./docs/LOCAL_DEVELOPMENT.md).
